# Contributing

Use one issue and one focused pull request per change. Do not merge a behavior change without tests and an explicit acceptance criterion.

Branch names use `feature/<issue>-<description>` or `fix/<issue>-<description>`.

Commit messages use Conventional Commits. Breaking contract changes require a migration note and coordinated version update.

